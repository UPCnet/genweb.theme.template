from plone.resource.traversal import ResourceTraverser


class {{{package.dottedname1}}}Traverser(ResourceTraverser):
    """The {{{package.dottedname1}}} theme traverser.

    Allows traversal to /++{{{package.dottedname1}}}++<name> using ``plone.resource`` to fetch
    things stored either on the filesystem or in the ZODB.
    """

    name = '{{{package.dottedname1}}}'
